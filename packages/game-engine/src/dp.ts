/**
 * Development points (Faz U1): everyone active is granted a fixed number of
 * points on in-game Sundays; points are spent on attribute XP or directly on
 * skill levels from the "Develop your character" page.
 */

/** Points granted per in-game Sunday. */
export const WEEKLY_DP = 5;

/** DP cost of one attribute training session. */
export const DP_ATTRIBUTE_COST = 2;
/** Attribute XP granted per training session. */
export const DP_ATTRIBUTE_XP = 120;

/** DP cost of raising a skill by one level. */
export const DP_SKILL_COST = 5;

/** Starting DP for a freshly created character (lets new players try it out). */
export const STARTING_DP = 10;
